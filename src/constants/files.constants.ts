export const MAX_FILE_SIZE_BYTES = 10 * 1024 * 1024;

/** Mime types accepted for upload; .jpg and .jpeg both report image/jpeg. */
export const ALLOWED_MIME_TYPES = ['image/png', 'image/jpeg'] as const;
